model Category {
  id String @id @default(uuid())

  name        String  @unique
  slug        String  @unique
  description String?
  image       String?

  gearItems GearItem[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("categories")
}
